node_modules
packages/*/package-lock.json
packages/*/pnpm-lock.yaml

# Environment files
.env
.env.local
.env.test
.env.production
.env.development
.env.*.local
.env.vercel.*
*.env

# AI / IDE tooling (local-only)
.claude/
# dep-sweep policy (sensitive/always_hold lists) is repo-level config — committed.
# release cadence docs live in docs/RELEASE_CADENCE.md. All other .claude/ state is local.
!.claude/dep-sweep-config.json
.serena/
.windsurf/
.data/
.sisyphus/
CLAUDE.md
AGENTS.md
opencode.jsonc
replit.md
.replit
.opencode/
.agents/
.agent-skills/
attached_assets/

# IDE — private configs stay in their own repos
.vscode
.cursor/
.cursorignore
.serena/
.idea/
*.swp
*.swo
*~

# Build outputs
dist
build
*.tsbuildinfo
packages/*/*.zip

# Session data
.worktrees/

# Downloads and logs
downloads/
logs/
*.log

# FFmpeg binaries
ffmpeg/bin/ffplay.exe
ffmpeg/bin/ffmpeg.exe
ffmpeg/bin/ffprobe.exe

# Generated files
/src/generated
packages/*/dist
packages/*/node_modules/.prisma
packages/shared/src/generated/

# Backup and test artifacts
src/webapp/frontend.backup/
packages/backend/src/public/

# Test coverage
coverage/
*.lcov
.nyc_output/

# Playwright
packages/frontend/playwright-report/
packages/frontend/test-results/
.playwright-mcp/

# Secrets and keys
*.pem
*.key
*.p12
*.pfx
*.jks
auth.json
secrets.json
config.json
*.secret

# Cloudflare Tunnel (credentials; config.example.yml is committed)
cloudflared/*.json
cloudflared/config.yml

# macOS / tooling caches
.DS_Store
.vercel
.cache_ggshield

# Local Claude Code spec scratch + worktrees (not part of repo)
.wt-specs/
worktrees/

# AI-generated docs / generator outputs — see
# decisions/2026-05-15-no-ai-generated-docs-in-tracked-state.md.
# Exact filenames only (no broad wildcards that would catch human-authored guides).
.github/copilot-instructions.md
.context/
packages/bot/TEST_MAP.md
docs/IMPLEMENTATION_STATUS.md
docs/LOVABLE_PROMPT.md
docs/FRONTEND_AI_STUDIO_CONTEXT.md
**/LIBRARY_RECOMMENDATIONS.md
**/CODE_EXAMPLES.md

# graphify knowledge-graph build artifacts (graph.json, GRAPH_REPORT.md, cache/, manifest.json, cost.json).
# Rebuild with `/graphify --update`; do not track.
graphify-out/


# Deploy auto-rollback state (must survive `git clean -fd` between deploys)
.deploy-last-good-sha

# Feature specs are local AI/planning artifacts — kept untracked (mirrored under .claude/specs/).
docs/specs/

# Stryker mutation-testing artifacts (generated)
.stryker-tmp/
packages/*/reports/mutation/
packages/*/reports/stryker-incremental.json
