name: Quality Gates

on:
    push:
        branches: ['release/**', main]
    pull_request:
        branches: ['release/**', main]

jobs:
    quality:
        uses: LucasSantana-Dev/.github/.github/workflows/quality.yml@6ec7fbeaa6f7a83dbe31dc6947ca5692e2d60d11
        with:
            node-version: '22'
            has-dockerfile: true
            run-deadcode: true
            package-manager: npm
        permissions:
            contents: read
            security-events: write
            actions: read
