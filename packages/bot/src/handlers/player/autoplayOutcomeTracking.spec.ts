// Tests for autoplay outcome tracking logic are covered in trackEventHandlers.spec.ts,
// which tests the playerStart/playerFinish/playerSkip handlers and their integration
// with the autoplay outcome classification. Removed tautological cache-only tests (#2301).
