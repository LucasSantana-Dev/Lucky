@@ -111,10 +111,20 @@
             boundOnProgress,
         )
 
-        // Mark as completed and store summary
-        await batchJobService.markCompleted(jobId)
+        // Check if executor signaled pause (e.g., client disconnect mid-loop)
+        if (summary && typeof summary === 'object' && (summary as { paused?: boolean }).paused === true) {
+            // Executor paused — mark as resumable, don't mark as completed
+            await batchJobService.markPaused(jobId)
+            infoLog({
+                message: 'Batch job paused',
+                data: { jobId, summary },
+            })
+        } else {
+            // Normal completion
+            await batchJobService.markCompleted(jobId)
+            infoLog({
+                message: 'Batch job completed successfully',
+                data: { jobId, summary },
+            })
+        }
         await batchJobService.setSummary(jobId, summary)
-
-        infoLog({
-            message: 'Batch job completed successfully',
-            data: { jobId, summary },
